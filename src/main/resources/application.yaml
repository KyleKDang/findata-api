spring:
  config:
    import: optional:file:./src/main/resources/application-secrets.yaml

  devtools:
    restart:
      enabled: false

  application:
    name: findata-api

  datasource:
    url: jdbc:postgresql://localhost:5432/financial_data
    username: postgres
    password: ${POSTGRES_PASSWORD:dev_password}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    validate-on-migrate: true

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.findata.api: DEBUG
    org.hibernate.SQL: DEBUG

alphavantage:
  api-key: ${ALPHAVANTAGE_API_KEY:demo}
  base-url: https://www.alphavantage.co/query